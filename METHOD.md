# Store Sales を縮約して解く

Favorita（エクアドル）の店×商品ファミリー×日の売上を、学習最終日の翌日から16日分予測する。指標は RMSLE。

## 人間が覚える5行

1. 指標が log なので、モデルも `log1p(sales)` を当てて戻す。
2. 16日先を一度に出すので、売上ラグは16日以上だけ使う（未来漏洩を防ぐ）。
3. LightGBM はプロモ・祝日・原油・給料日・店属性を、店をまたいで学ぶ。
4. Amazon Chronos-2 は系列の形と、未来に分かる共変量をゼロショットで読む。
5. Google TimesFM 2.5 は系列の数値だけを読む。AWS に依存しない対照。検証 RMSLE の逆数で混ぜる。

## 検証

学習の末尾16日を切り、テストと同じ長さで測る。ここで負けたモデルの重みは自然に小さくなる。

## 提出

`outputs/submission.csv` の列は `id,sales`。公式データは `data/kaggle/` に置く。
